# ROS2 / colcon
build/
install/
log/
# Python / venv
__pycache__/
*.pyc
.venv*/
# Results & artifacts
results/*/
# 'results' itself may be a symlink to a big drive (machine-specific) — never track it
/results
*.html
# Large binaries / weights / vocab (tracked explicitly where needed)
*.engine
*.onnx
*.pt
*.bin
# Editor / agent
.vscode/
.idea/
*.swp
*~
.claude/
